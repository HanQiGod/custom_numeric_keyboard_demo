# custom_numeric_keyboard_demo

一个自定义数字键盘 Flutter 示例项目，包含：

- 自定义数字键盘组件
- 金额输入模式（支持两位小数）
- 删除、长按清空、收起、完成等基础交互

## 目录结构

```text
lib/
  main.dart
  src/
    app.dart
    pages/
      number_keyboard_page.dart
    widgets/
      hs_number_keyboard.dart
```
